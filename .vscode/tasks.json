{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "docker-compose: up (debug)",
      "type": "shell",
      "command": "docker compose -f docker-compose.yml -f docker-compose.debug.yml up -d --build db backend",
      "problemMatcher": []
    }
  ]
}
